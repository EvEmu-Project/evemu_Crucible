DATABASE INSTALLATION GUIDE:
=============================
 1. Download official EVE database dump:
     http://eve.no-ip.de/apo100/apo100-mysql5-v1.sql.bz2
    Unpack it - you should get file with ".sql" extension.

 2. Create database into which you want to stick all the EVEmu DB stuff.

 3. Import the official dump (the one you downloaded and unpacked).

 4. Go to subdirectory ./ofic-updates and:
    a) If you are on Windows:
        Execute merge-sql.bat.
    b) If you are on Linux:
        Execute merge-sql.sh.
    Now import file ofic-update.sql, which the script generated.

 5. Import evemu_static-dump.sql.

 6. Import evemu_dynamic-dump.sql.

 7. Import all the updates from subdirectory ./evemu-updates.

 8. Import prime_db.sql.

FILE DESCRIPTION:
===================
evemu_static-dump.sql
**********************
This file contains static data required by EVEmu.

evemu_dynamic-dump.sql
***********************
This file contains dynamic database schema required by EVEmu.

prime_db.sql
*************
This is special SQL batch, which is intended to reset all the dynamic stuff into default state.
